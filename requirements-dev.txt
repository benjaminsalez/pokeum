# Local development tooling only — not part of the runtime dependencies.
-r requirements.txt

ruff==0.15.16
pytest==8.3.3
mypy==2.1.0

# Git-hook runner: `pre-commit install` once per clone, then every commit is
# linted, formatted, type-checked, secret-scanned and tested automatically.
pre-commit==4.6.0
detect-secrets==1.5.0

# Dependency CVE scanner: `python -m pip_audit -r requirements.txt`.
pip-audit==2.10.0

# Static security scanner for our own code (the `toml` extra reads the
# [tool.bandit] section in pyproject.toml):
#     python -m bandit -c pyproject.toml -r app main.py
bandit[toml]==1.9.4

# OPTIONAL context-compression proxy for Claude Code sessions: compresses tool
# output, logs, and history before they reach the model (reversible; originals
# retrievable on demand). Start a compressed session with scripts/claude-headroom.
headroom-ai[all]==0.30.0
