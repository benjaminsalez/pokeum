# Example environment variables for local development. Copy this file to `.env`
# and fill in real values; `.env` is gitignored and never committed.
#
# The app reads configuration from the process environment through
# app/core/config.py — see openwiki/workflow.md. `python main.py ...` loads
# this file automatically at startup (python-dotenv); variables already set in
# your shell or platform always take precedence. Every key below is optional;
# the app runs with sensible defaults.

# Logging: `debug` shows everything our code does, `info` only milestones.
LOG_LEVEL=info

# `true` emits one-line JSON log records (handy for log shippers).
LOG_JSON=false

# --- Reference data & artifacts --------------------------------------------
# Root directory for the local reference store, image cache, and index
# artifacts. Gitignored; created on first `sync`.
DATA_DIR=./data

# TCGdex REST API base (free, no key). Fallback: https://api.pokemontcg.io/v2
# (different response shape — not a drop-in).
TCGDEX_BASE_URL=https://api.tcgdex.net/v2

# Card language to sync and recognize. The reference data and OCR are built for
# this language; `en` is the only fully supported value today.
CARD_LANGUAGE=en

# Path to an exported ONNX image encoder. The default is the fine-tuned
# DINOv2-small (photography-invariance training; see openwiki/training.md).
# When the file is absent, recognition falls back to the built-in pure-numpy
# HistogramEmbedder, so the system still runs — just far less robust to glare
# and extreme angles.
EMBED_MODEL_PATH=./data/models/dinov2s-ft-v1.onnx

# --- API service ------------------------------------------------------------
API_HOST=127.0.0.1
API_PORT=8000
# Built frontend (frontend/dist) served by the API at the root, making the
# service a single origin (the API itself lives under /api). Serving activates
# only when the directory exists; set empty to disable even when it does.
FRONTEND_DIST_DIR=./frontend/dist

# --- Webcam -----------------------------------------------------------------
# OpenCV capture device index for `pokeum scan`.
WEBCAM_INDEX=0

# --- Scan diagnostics ---------------------------------------------------------
# When set, every /identify frame that fails card detection is saved here as a
# JPEG so you can inspect exactly what the camera sent. Diagnostic tool — leave
# empty in normal use.
SCAN_DEBUG_DIR=

# --- Scan collection (S3) -----------------------------------------------------
# When a user saves a scan (and has consented in the UI), the captured photo and
# its match annotation upload to this bucket in the background as training data.
# Images are normalized server-side (SCAN_STORE_* constants) and uploads pause
# automatically while the bucket is at/above SCANS_MAX_BUCKET_BYTES (100 GB).
# Leave the bucket empty to disable collection entirely — the app behaves
# identically, uploads are just dropped with a debug log.
SCANS_S3_BUCKET=
# Region of the bucket; empty defers to boto3's default resolution. For
# Cloudflare R2 use "auto" (location hints like WEUR only apply at creation).
SCANS_S3_REGION=
# Key prefix inside the bucket; objects land at <prefix>/YYYY/MM/DD/<uuid>.jpg
# with a sibling .json annotation.
SCANS_S3_PREFIX=scans
# Custom endpoint for S3-compatible stores (MinIO, R2, ...); empty = AWS S3.
SCANS_S3_ENDPOINT_URL=
# Credentials are read by boto3's standard chain (env vars, shared credentials
# file, instance role) — uncomment to supply them via this file's loader:
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_DEFAULT_REGION=
