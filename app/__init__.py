"""Application package.

Rename this package to your project and build your code inside it. ``app.core``
holds the shared foundation (configuration, constants, logging) that every other
module may import; see ``openwiki/quickstart.md``.
"""
